{% extends "base.html" %}

{% block content %}
<section class="bus-details-section">
    <h2>Bus Details</h2>

    {% if busd %}
        <div class="bus-details">
            <p><strong>Bus ID:</strong> {{ busd[0] }}</p>
            <p><strong>Origin:</strong> {{ busd[1].title() }}</p>
            <p><strong>Destination:</strong> {{ busd[2].title() }}</p>
            <p><strong>Cost:</strong> Ksh {{ busd[3] }}</p>
            <p><strong>Rating:</strong> {{ busd[4] }}</p>
            <p><strong>Date:</strong> {{ busd[8] }}</p>
            <p><strong>Departure Time:</strong> {{ busd[5] }}</p>
            <p><strong>Arrival Time:</strong> {{ busd[6] }}</p>
            <p><strong>Seats Available:</strong> {{ busd[7] }}</p>
        </div>

        <form action="{{ url_for('booked', busid=busd[0]) }}" method="POST" class="booking-form">
            <h3>Book Your Ticket</h3>

            <label for="name">Name:</label>
            <input type="text" id="name" name="name" value="{{ user_data.name }}" required><br><br>

            <label for="phone">Phone Number:</label>
            <input type="text" id="phone" name="phone" value="{{ user_data.phone }}" required><br><br>

            <label for="email">Email:</label>
            <input type="email" id="email" name="email" value="{{ user_data.email }}" required><br><br>

            <label for="passengers">Number of Passengers:</label>
            <select id="passengers" name="passengers" required>
                {% for i in range(1, busd[7] + 1) %}
                    <option value="{{ i }}">{{ i }}</option>
                {% endfor %}
            </select><br><br>

            <input type="submit" name="submit" value="Book Ticket" class="submit-button">
        </form>
    {% else %}
        <p>Bus details not available.</p>
    {% endif %}
</section>
{% endblock %}
